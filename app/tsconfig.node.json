{
  "//": "Config for the build-tool files, which run in Node rather than the browser. @vue/tsconfig ships no tsconfig.node.json, so the Node-specific bits are set explicitly here.",
  "extends": "@vue/tsconfig/tsconfig.json",
  "include": ["vite.config.ts", "capacitor.config.ts"],
  "compilerOptions": {
    "composite": true,
    "noEmit": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node"]
  }
}
