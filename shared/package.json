{
  "name": "shared",
  "version": "0.0.0",
  "private": true,
  "description": "Wire-format contracts shared by the app and the Cloud Functions API.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "//": "npm runs `prepare` for workspace packages on a root install, so a fresh clone gets dist/ without a separate build step.",
    "prepare": "tsc"
  },
  "dependencies": {
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
